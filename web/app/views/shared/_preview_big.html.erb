<fieldset>
  <legend>LaTeX Preview</legend>
  <strong>Note:</strong> Due to pages being cropped and appended, some lines may have incorrect spacing.<br/><br/>
  <%= render :partial => "shared/preview" %>
</fieldset>
