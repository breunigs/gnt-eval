<%= @anrede.word_wrap %>,

bitte lies diese Mail ganz genau.

Du bist in der Datenbank eingetragen, in der kommenden Woche am

	<%= @course.description %>

die Veranstaltung

	<%= @course.title %>

bei

	<%= @course.profs.map{ |p| p.fullname }.join(', ') %>

zu evaluieren.

Eingetragen in der Datenbank ist die Verwendung von

	<%= @course.students %> Bögen des Typs <%= @course.form.name %>, <%= @course.language.to_s %>

Wenn hier Daten fehlen fehlen oder falsch sind, melde dich bitte
sobald wie möglich bei uns -- am besten gleich mit den richtigen
Daten.

Die Bögen findest du im Fachschaftsraum. Die Mappen sind farblich
kodiert für den Wochentag und dann beschriftet. Obenauf liegt ein
Howto, um dich noch an die wichtigsten Dinge zu erinnern.

Auf eine Sache achte bitte ganz genau: Manche Veranstaltungen sind bei
mehreren Dozent_innen. Die Übungsgruppen dürfen aber nur pro Person
EIN MAL evaluiert werden. Also GANZ GENAU drauf hinweisen.

Bei Problemen jeglicher Art antworte bitte umgehend auf diese E-Mail.

Vielen Dank,
Oliver
