<div class="group">
  <h2>Editing <%=Semester.model_name.human%> <%=h @semester.title %></h2>
  <%= render "form" %>
</div>

<div>
  <h3>Courses</h3>
  This term has <%=@semester.courses.size%> courses. <%=link_to "You can view them if you want", semester_courses_path(@semester)%>. If you want to work on this semester it’s highly recommended to make it active, i.e. ensure that the current date lies in between the term’s start and end date. Otherwise handling will be unnecessarily complicated.
</div>


<div>
  <h3>rake Actions</h3>
  <pre>
# Generate forms:
rake forms:generate[<%=@semester.id%>]

# Generate result PDFs for each faculty:
<% @semester.faculties.each do |f| %>
rake results:pdf_report["en", <%=@semester.id%>, <%=f.id%>]
<% end %>

# Alternatively, generate result PDFs for each course:
<% @semester.faculties.each do |f| %>
rake results:pdf_singles[<%=@semester.id%>, <%=f.id%>]
<% end %></pre>
</div>
